/*******************************************
描述：这是一个插入算法原型
功能：InsertionSort插入函数，实现数组中插入一个数字
版本：v1.00
作者：汤俊伟
时间：2017.05.20
学习点：插入算法
********************************************/

void InsertionSort(ElementType arr[],int N)
{
    int i,j,tmp;
    for(i = 1;i<n;i++)
    {
        tmp = arr[i];                 //提取一个数字
        for(j = i;j>0&&arr[j-1]>tmp;j--)  //当输入数列前边都小于后边时，始终不进入for，因此顺序不变。
            arr[j] = arr[j-1];        //移出空位
        arr[j] = tmp;                     //插入数字
    }
    
}
